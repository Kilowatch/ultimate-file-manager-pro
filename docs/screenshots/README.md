# 📸 README Screenshots — Drop-In Guide

This folder holds the screenshots shown in the README's **`## 📸 Screenshots`** section.
The README already references the filenames below — you only need to **copy the image files here** (keeping these exact filenames) and the section renders automatically.

> ✅ **To add a screenshot:** save the file into this folder with the exact filename shown below, then commit. Done — no other edits needed.

## Slots

| File to place here | Screen to capture | Orientation / size |
|---|---|---|
| `mobile-main.jpeg` | 📱 Mobile — **main file manager screen** (the primary home view) | Portrait, ~1080×2340 (or any 9:19.5 crop) |
| `mobile-cloud.jpeg` | 📱 Mobile — **network & cloud storage** view (SMB / SFTP / WebDAV / cloud entries) | Portrait, ~1080×2340 |
| `mobile-storage-access.jpeg` | 📱 Mobile — **storage access** screen (where the user grants folder / full access) | Portrait, ~1080×2340 |
| `tv-browser.jpeg` | 📺 TV — **big-screen browser** (Android TV / Fire TV D-pad interface) | **Landscape, 16:9** (e.g. 1280×720) |

## Tips

- Prefer clean captures with no status-bar clutter if possible; a plain dark backdrop matches the app's branding.
- Keep the filenames **exact** — the README and the HTML comment above the Screenshots section reference them.
- Everything already in the README will render automatically once these files exist; nothing else needs to change.
