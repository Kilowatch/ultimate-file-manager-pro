<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Night theme inherits from base - already dark by default -->
    <style name="Theme.UltimateFileManager" parent="Base.Theme.UltimateFileManager" />
    <style name="Theme.UltimateFileManager.Tv" parent="Base.Theme.UltimateFileManager.Tv" />

    <!-- Player theme: Always black background to avoid glitches and bleed-through -->
    <style name="Theme.UFM.Player" parent="Theme.UltimateFileManager">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>