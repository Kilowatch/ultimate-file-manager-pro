<?xml version="1.0" encoding="utf-8"?>
<!--
    Focus/press selector for TV colour swatches.
    Focused: yellow highlight border to indicate D-Pad focus.
    Default: rounded rectangle with no border.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Focused (D-Pad hover) -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="3dp"
                android:color="@color/tv_accent" />
            <corners android:radius="6dp" />
        </shape>
    </item>

    <!-- Pressed -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/tv_glass_white_20" />
            <corners android:radius="6dp" />
        </shape>
    </item>

    <!-- Default -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="6dp" />
        </shape>
    </item>

</selector>
