<?xml version="1.0" encoding="utf-8"?>
<!-- One-time recovery code display — shown after PIN is set. Cannot be dismissed without tapping Done. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:background="@color/tv_bg_gradient_end">

    <!-- Lock icon -->
    <ImageView
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/ic_lock"
        android:tint="@color/tv_button_focused_yellow"
        android:layout_marginBottom="12dp" />

    <!-- Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/vault_recovery_code_title"
        android:textColor="@color/tv_text_primary"
        android:textSize="18sp"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:layout_marginBottom="8dp" />

    <!-- Warning body -->
    <TextView
        android:id="@+id/txtRecoveryBody"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/tv_text_secondary"
        android:textSize="13sp"
        android:gravity="center"
        android:lineSpacingMultiplier="1.3"
        android:layout_marginBottom="20dp" />

    <!-- The code itself — large monospace font -->
    <TextView
        android:id="@+id/txtCode"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:textSize="22sp"
        android:textColor="@color/tv_button_focused_yellow"
        android:textStyle="bold"
        android:gravity="center"
        android:letterSpacing="0.12"
        android:background="#1AFFFFFF"
        android:padding="12dp"
        android:layout_marginBottom="12dp" />

    <!-- Copy button -->
    <Button
        android:id="@+id/btnCopyCode"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="#26FFFFFF"
        android:textColor="@color/tv_text_primary"
        android:textSize="13sp"
        android:layout_marginBottom="4dp" />

</LinearLayout>
