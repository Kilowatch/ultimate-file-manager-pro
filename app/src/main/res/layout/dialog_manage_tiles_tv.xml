<?xml version="1.0" encoding="utf-8"?>
<!--
    TV Manage Tiles Dialog
    Full-width, dark glass panel with a scrollable tile list (level 1)
    and a slide-in action bar with Restore/Open buttons (level 2).
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/tv_dialog_background">

    <!-- ── Header ─────────────────────────────────────────────────────────── -->
    <LinearLayout
        android:id="@+id/headerLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="48dp"
        android:paddingTop="40dp"
        android:paddingBottom="20dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-black"
            android:text="@string/manage_tiles_title"
            android:textColor="@color/tv_text_primary"
            android:textSize="42sp" />

        <View
            android:layout_width="60dp"
            android:layout_height="3dp"
            android:layout_marginTop="8dp"
            android:background="@color/tv_accent" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="@string/select_a_tile_ok_to"
            android:textColor="@color/tv_text_secondary"
            android:alpha="0.8"
            android:textSize="18sp" />
    </LinearLayout>

    <!-- ── Horizontal divider ─────────────────────────────────────────────── -->
    <View
        android:id="@+id/headerDivider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/tv_glass_border"
        app:layout_constraintTop_toBottomOf="@id/headerLayout"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- ── Empty state ────────────────────────────────────────────────────── -->
    <TextView
        android:id="@+id/txtNoHiddenTiles"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="48dp"
        android:text="@string/manage_tiles_no_hidden"
        android:textColor="@color/tv_text_secondary"
        android:textSize="22sp"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/headerDivider" />

    <!-- ── Tile list ──────────────────────────────────────────────────────── -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerHiddenTiles"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:paddingHorizontal="24dp"
        android:paddingVertical="16dp"
        android:clipToPadding="false"
        app:layout_constraintTop_toBottomOf="@id/headerDivider"
        app:layout_constraintBottom_toTopOf="@id/actionDivider"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ── Divider above action panel ────────────────────────────────────── -->
    <View
        android:id="@+id/actionDivider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/tv_glass_border"
        app:layout_constraintBottom_toTopOf="@id/actionPanel"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- ── Action Panel (Restore All / Exit) ─────────────────────────────── -->
    <LinearLayout
        android:id="@+id/actionPanel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end|center_vertical"
        android:paddingHorizontal="48dp"
        android:paddingVertical="24dp"
        android:background="#1AFFFFFF"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <!-- Selected tile name label (shown when a tile is selected) -->
        <TextView
            android:id="@+id/txtSelectedTile"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/tv_text_primary"
            android:textSize="20sp"
            android:fontFamily="sans-serif-medium"
            android:ellipsize="end"
            android:maxLines="1"
            android:visibility="gone" />

        <!-- Open button (shown when a tile is selected) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnOpenTile"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="wrap_content"
            android:layout_height="56dp"
            android:text="@string/open"
            android:fontFamily="sans-serif-black"
            android:textSize="16sp"
            android:textColor="@color/selector_tv_button_text"
            app:backgroundTint="@color/selector_tv_button_yellow"
            app:cornerRadius="16dp"
            android:focusable="true"
            android:visibility="gone"
            android:layout_marginEnd="16dp" />

        <!-- Cancel button (shown when a tile is selected) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnCancelAction"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="wrap_content"
            android:layout_height="56dp"
            android:text="@string/cancel_1"
            android:fontFamily="sans-serif-black"
            android:textSize="16sp"
            android:textColor="@color/selector_tv_button_text"
            app:backgroundTint="@color/selector_tv_button_yellow"
            app:cornerRadius="16dp"
            android:focusable="true"
            android:visibility="gone"
            android:layout_marginEnd="16dp" />

        <!-- Restore button (shown when a tile is selected) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnRestoreTile"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="wrap_content"
            android:layout_height="56dp"
            android:text="@string/restore"
            android:fontFamily="sans-serif-black"
            android:textSize="16sp"
            android:textColor="@color/selector_tv_button_text"
            app:backgroundTint="@color/selector_tv_button_yellow"
            app:cornerRadius="16dp"
            android:focusable="true"
            android:visibility="gone"
            android:layout_marginEnd="16dp" />

        <!-- Restore All button (shown when no tile is selected) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnRestoreAll"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="wrap_content"
            android:layout_height="56dp"
            android:text="@string/manage_tiles_restore_all"
            android:fontFamily="sans-serif-black"
            android:textSize="16sp"
            android:textColor="@color/selector_tv_button_text"
            app:backgroundTint="@color/selector_tv_button_yellow"
            app:cornerRadius="16dp"
            android:focusable="true"
            android:visibility="gone" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
