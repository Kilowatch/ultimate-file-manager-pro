<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="56dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="8dp"
    android:paddingEnd="16dp"
    android:clickable="true"
    android:focusable="true"
    android:background="?android:attr/selectableItemBackground">

    <!-- Drag handle -->
    <ImageView
        android:id="@+id/imgDragHandle"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_more_vert"
        android:importantForAccessibility="no"
        app:tint="@color/ufm_text_secondary" />

    <!-- Track number -->
    <TextView
        android:id="@+id/txtTrackIndex"
        android:layout_width="28dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="12sp"
        android:textColor="@color/ufm_text_secondary"
        android:text="1" />

    <!-- Playing indicator (shown when this item is current) -->
    <ImageView
        android:id="@+id/imgPlayingIndicator"
        android:layout_width="12dp"
        android:layout_height="12dp"
        android:layout_marginEnd="4dp"
        android:src="@drawable/ic_play"
        android:importantForAccessibility="no"
        android:visibility="gone"
        app:tint="@color/ufm_primary" />

    <!-- Title -->
    <TextView
        android:id="@+id/txtQueueItemTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="8dp"
        android:textSize="14sp"
        android:textColor="@android:color/white"
        android:maxLines="1"
        android:ellipsize="end" />

    <!-- Duration -->
    <TextView
        android:id="@+id/txtQueueItemDuration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:textSize="12sp"
        android:textColor="@color/ufm_text_secondary" />
</LinearLayout>
