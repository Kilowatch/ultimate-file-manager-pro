<?xml version="1.0" encoding="utf-8"?>
<!--
    Mobile Permission Card — premium dark-glass design
    Layout: [AccentCircleIcon] [Title + Badge] [Description] [Grant Button]
    The card sits on a dark gradient so we use white-glass with visible border.
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cardPermission"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/margin_md"
    android:layout_marginVertical="6dp"
    android:clickable="true"
    android:focusable="true"
    app:cardBackgroundColor="@color/mobile_glass_card"
    app:cardCornerRadius="20dp"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="@color/mobile_glass_stroke"
    app:rippleColor="@color/mobile_glass_ripple">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/margin_md">

        <!-- Top row: Icon + Title + Badge + Grant Button -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Accent circle icon -->
            <FrameLayout
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/bg_icon_circle_accent">

                <ImageView
                    android:id="@+id/imgPermIcon"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:layout_gravity="center"
                    android:contentDescription="@string/cd_permission_icon"
                    app:tint="@color/mobile_icon_tint"
                    tools:src="@drawable/ic_folder" />
            </FrameLayout>

            <!-- Title + badge column -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:layout_marginHorizontal="14dp">

                <TextView
                    android:id="@+id/txtPermTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@color/mobile_text_primary"
                    android:textSize="16sp"
                    android:fontFamily="sans-serif-medium"
                    android:maxLines="1"
                    android:ellipsize="end"
                    tools:text="Storage Access" />

                <!-- Status badge — single line pill, never wraps -->
                <TextView
                    android:id="@+id/txtPermStatus"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/bg_status_badge_required"
                    android:paddingHorizontal="10dp"
                    android:paddingVertical="3dp"
                    android:textSize="10sp"
                    android:fontFamily="sans-serif-medium"
                    android:textAllCaps="true"
                    android:letterSpacing="0.06"
                    android:maxLines="1"
                    android:singleLine="true"
                    tools:text="Required"
                    tools:textColor="#FFFFCC00" />

            </LinearLayout>

            <!-- Grant Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnGrant"
                android:layout_width="wrap_content"
                android:layout_height="40dp"
                android:text="@string/btn_grant"
                android:textSize="13sp"
                android:textColor="@color/mobile_text_primary"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                app:backgroundTint="#FF0284C7"
                app:cornerRadius="20dp" />

        </LinearLayout>

        <!-- Description — full width below, no truncation -->
        <TextView
            android:id="@+id/txtPermDesc"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:layout_marginStart="62dp"
            android:textColor="@color/mobile_card_text_secondary"
            android:textSize="13sp"
            android:lineSpacingMultiplier="1.4"
            android:maxLines="3"
            android:ellipsize="end"
            tools:text="Required to browse, copy, move and manage files on your device storage." />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
